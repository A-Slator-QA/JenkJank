pipeline {
    agent any
    stages {
        stage('Running shell scripts') {
            steps {
                sh '''
                bash ./hello.sh
                '''
            }
        }
        
    }
}
